[project]
name = "learning"
version = "0.1.0"
description = "Employee performance evaluator using OpenAI"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.0.0",
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
]
